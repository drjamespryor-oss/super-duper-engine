import React, { useState } from 'react';

// Define TypeScript interfaces for our financial data structures
interface FinancialMetric {
  label: string;
  value: string;
  description: string;
  category: 'revenue' | 'liquidity' | 'risk' | 'debt';
}

interface FinancialReport {
  title: string;
  date: string;
  url: string;
}

export const FinancialPerformanceDashboard: React.FC = () => {
  // 1. State Management for Feedback Form
  const [feedback, setFeedback] = useState({ name: '', email: '', message: '' });
  const [formSubmitted, setFormSubmitted] = useState(false);

  // 2. Mock Data based on Adventist Health 2025/2026 disclosures
  const metrics: FinancialMetric[] = [
    { label: 'Total Revenue', value: '$7.4 Billion', description: 'Total annual revenue generated', category: 'revenue' },
    { label: '10-Year Capital Spend', value: '$2 Billion', description: 'Long-term investment back into communities', category: 'revenue' },
    { label: 'EBIDA', value: '$224 Million', description: 'Earnings Before Interest, Depreciation, and Amortization', category: 'revenue' },
    { label: 'Cash & Investments', value: '$3.4 Billion', description: 'Total liquid reserves and investment portfolio', category: 'liquidity' },
    { label: 'Days Cash on Hand', value: '150 Days', description: 'Number of days organization can fund expenses with cash', category: 'liquidity' },
    { label: 'Debt-to-Capitalization', value: '62.2%', description: 'Ratio of total debt to total capital structure', category: 'debt' },
    { label: 'At-Risk Lives', value: '229K+', description: 'Individuals covered under value-based care contracts', category: 'risk' },
  ];

  const reports: FinancialReport[] = [
    { title: '2025 Annual Audit Report', date: 'Dec 31, 2025', url: '#audit-2025' },
    { title: '2026 Q1 Financial Statements and MD&A', date: 'Q1 2026', url: '#q1-2026' },
    { title: 'Liquidity Report', date: 'Current', url: '#liquidity' },
  ];

  // 3. Form Submission Handler
  const handleFeedbackSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    // In production, send data to your API route
    console.log('Feedback submitted:', feedback);
    setFormSubmitted(true);
    // Reset form field values
    setFeedback({ name: '', email: '', message: '' });
  };

  return (
    <div className="min-h-screen bg-gray-50 text-gray-900 font-sans antialiased">
      
      {/* ACCESSIBILITY: Skip to Content Link */}
      <a 
        href="#main-content" 
        className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-blue-600 text-white px-4 py-2 rounded shadow-md z-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
      >
        Skip to Content ↵ ENTER
      </a>

      {/* Header Banner */}
      <header className="bg-white border-b border-gray-200" role="banner">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
          <div>
            <span className="text-xs font-bold uppercase tracking-wider text-blue-600">Adventist Health</span>
            <h1 className="text-3xl font-extrabold tracking-tight text-gray-900">Financial Performance</h1>
          </div>
          <div className="flex items-center gap-2 bg-blue-50 text-blue-800 p-3 rounded-lg border border-blue-100 text-sm">
            <span className="font-semibold" aria-hidden="true">💡 Screen-Reader Guide:</span>
            <p>Use heading navigation (H2/H3) and ARIA grid structure to review performance data metrics.</p>
          </div>
        </div>
      </header>

      {/* Main Content Area */}
      <main id="main-content" className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10" tabIndex={-1}>
        
        {/* Mission Statement Section */}
        <section aria-labelledby="mission-title" className="mb-12 bg-gradient-to-r from-blue-700 to-indigo-800 text-white rounded-2xl p-8 shadow-xl">
          <h2 id="mission-title" className="text-2xl font-bold mb-4">Our Investment in Communities</h2>
          <p className="text-lg text-blue-100 max-w-3xl leading-relaxed">
            Adventist Health is a nonprofit health system. A strong financial performance allows us to bring our mission to life through investing back into the communities we serve, enhancing care, and growing our services. We are committed to financial transparency for those who support this work.
          </p>
        </section>

        {/* Financial At-A-Glance Metric Grid */}
        <section aria-labelledby="metrics-title" className="mb-12">
          <div className="flex flex-col mb-6">
            <h2 id="metrics-title" className="text-2xl font-bold text-gray-900">Financial At-a-Glance</h2>
            <p className="text-sm text-gray-600 mt-1">Data audited and compiled as of December 31, 2025.</p>
          </div>

          {/* ARIA Layout: Data Grid */}
          <div 
            role="grid" 
            aria-readonly="true" 
            aria-label="Financial Metrics Data Matrix"
            className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6"
          >
            {metrics.map((metric, idx) => (
              <div 
                key={idx}
                role="row"
                className="bg-white p-6 rounded-xl border border-gray-200 shadow-sm flex flex-col justify-between hover:shadow-md transition-shadow focus-within:ring-2 focus-within:ring-blue-500"
              >
                <div role="gridcell" className="flex flex-col">
                  {/* Label element connects directly to semantic metrics usage */}
                  <span className="text-sm font-medium text-gray-500 uppercase tracking-wide">
                    {metric.label}
                  </span>
                  <span className="text-3xl font-bold text-gray-900 mt-2 tracking-tight">
                    {metric.value}
                  </span>
                </div>
                <div role="gridcell" className="mt-4 pt-4 border-t border-gray-100">
                  <p className="text-xs text-gray-600 italic">
                    {metric.description}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </section>

        {/* Financial Disclosure Reports Column */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
          
          <section aria-labelledby="reports-title" className="lg:col-span-2 bg-white p-8 rounded-2xl border border-gray-200 shadow-sm">
            <h2 id="reports-title" className="text-2xl font-bold text-gray-900 mb-2">Financial Investor Disclosures</h2>
            <p className="text-gray-600 mb-6 text-sm">Review full public accounting records, rating statements, and operational audits.</p>
            
            <ul className="space-y-4" role="list">
              {reports.map((report, idx) => (
                <li key={idx} className="border border-gray-100 rounded-xl p-4 hover:bg-gray-50 transition-colors">
                  <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                      <h3 className="font-semibold text-gray-900">{report.title}</h3>
                      <p className="text-xs text-gray-500 mt-0.5">Filing Window Period: {report.date}</p>
                    </div>
                    <a
                      href={report.url}
                      className="inline-flex items-center justify-center px-4 py-2 border border-blue-600 text-sm font-medium rounded-lg text-blue-600 bg-white hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors"
                      aria-label={`Download document: ${report.title} (opens in new window)`}
                      target="_blank"
                      rel="noopener noreferrer"
                    >
                      View Report
                      <span className="ml-1.5" aria-hidden="true">↗</span>
                    </a>
                  </div>
                </li>
              ))}
            </ul>
          </section>

          {/* Feedback & Compliance Form Section */}
          <section aria-labelledby="feedback-title" className="bg-white p-8 rounded-2xl border border-gray-200 shadow-sm">
            <h2 id="feedback-title" className="text-xl font-bold text-gray-900 mb-2">Transparency Feedback</h2>
            <p className="text-xs text-gray-600 mb-6">Report digital accessibility problems or question investor reporting metrics.</p>

            {formSubmitted ? (
              <div 
                role="alert" 
                className="bg-green-50 border border-green-200 text-green-800 p-4 rounded-xl text-sm"
              >
                <p className="font-bold">Thank you for your report.</p>
                <p className="mt-1">Our compliance team will review your submission and answer within 2 business days.</p>
                <button 
                  onClick={() => setFormSubmitted(false)}
                  className="mt-3 text-xs font-semibold text-green-700 underline block hover:text-green-900"
                >
                  Submit another entry
                </button>
              </div>
            ) : (
              <form onSubmit={handleFeedbackSubmit} className="space-y-4">
                <div>
                  <label htmlFor="form-name" className="block text-xs font-semibold uppercase text-gray-700 tracking-wider mb-1">
                    Your Name
                  </label>
                  <input
                    id="form-name"
                    type="text"
                    required
                    value={feedback.name}
                    onChange={(e) => setFeedback({ ...feedback, name: e.target.value })}
                    className="w-full border border-gray-300 rounded-lg p-2.5 text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                  />
                </div>

